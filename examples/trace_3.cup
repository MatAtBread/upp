@include(trace.hup)

@trace int my_function(int x, char *y) {
    int g = 1;
    for (int i = 1; i < x; i++) {
        g = g * i;
    }
    return g;
}

int main() {
    printf("magic number %d\n", my_function(10,"Foo"));
    return 0;
}
