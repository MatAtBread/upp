@define transform_if() {
    upp.withMatch(upp.root, "if ($cond) $then__NOT_compound_statement;",
        ({ cond, then }) => upp.code`if (${cond}) { ${then} }`);
}

@transform_if;

int main() {
    if (1) return 0;
    if (2) {
        return 0;
    }
}
