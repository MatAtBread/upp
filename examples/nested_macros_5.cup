#include "io-lite.h"

static int expanded_inner = 5;

@define inner(y) {
    return upp.codeTree`expanded_inner + ${y}`;
}

@define outer(x) {
    return upp.codeTree`${upp.consume()} + ${x}`;
}

int main() {
    int n = @outer(20) @inner(10);
    printf("%d\n", n);
    return 0;
}
