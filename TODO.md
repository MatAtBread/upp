Specific use cases:
    @Refcount
    @generic(type): for example boxes, lists, vectors

General improvements:
    Sequential: @A @B @C

Environment:
    In the test_runner, if a translation succeeds, compile and run the test. Keep the output for comparison
    Option to upp, compile, run