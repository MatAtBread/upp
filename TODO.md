Specific use cases:
    @String type
    @generic(type): for example boxes, lists, vectors

General improvements:
    Sequential: @A @B @C
    Ensure externs work, eg for @method
    Modify hoist() so functions and data defintions are hoisted before the first function defintion and struct defintions above that

Environment:
    Option to upp, compile, run